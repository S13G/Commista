      # data = {
        #     'product_id': str(self.product.id),
        #     'ratings': 4,
        #     'description': 'Good product!',
        # }
        #
        # # Attach image files to the request
        # image_files = [
        #     open('static/pic1.jpeg', 'rb'),
        #     open('static/pic2.jpeg', 'rb'),
        # ]
        # for i, image_file in enumerate(image_files):
        #     data[f'images[{i}][image]'] = image_file
        #
        # # Send the request
        # url = reverse_lazy('add_product_review')
        # response = self.client.post(url, data, format='multipart')
        # print(response.data)
        #
        # self.assertEqual(response.status_code, status.HTTP_201_CREATED)
        # self.assertEqual(response.data['status'], 'success')
        # self.assertEqual(response.data['message'], 'Review created successfully')
        #
        # product_review = ProductReview.objects.get(product=self.product, customer=self.user)
        # self.assertEqual(product_review.product_review_images.count(), len(image_files))
        #
        # # Clean up the image files
        # for image_file in image_files:
        #     image_file.close()
